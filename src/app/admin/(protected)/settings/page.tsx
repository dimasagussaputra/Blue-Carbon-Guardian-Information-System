import SettingsPage from "@/components/admin/Settings/SettingsPage";

export const metadata = {
  title: "Settings | Admin BCG",
};

export default function SettingsRoute() {
  return <SettingsPage />;
}
